predicates
hobi (symbol,symbol) - nondeterm (o,i), nondeterm (i,i)

clauses
  hobi(neni,menyanyi).
  hobi(mahatir,coding).
  hobi(budi,bulu_tangkis).
  hobi(hamri,dota).
  hobi(fegy,vainglory).
  hobi(karin,membaca).

goal
  hobi(Person,membaca);
  hobi(Person2,vainglory).